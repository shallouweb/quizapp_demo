name: test_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  #2024.12.26追加したパッケージ
  shared_preferences: ^2.0.15  # 最新バージョンにしてください
  audioplayers: ^0.20.1
  cupertino_icons: ^1.0.8

flutter:
  assets:
    - assets/correct_answer.mp3
    - assets/wrong_answer.mp3

  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
